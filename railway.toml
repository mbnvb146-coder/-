[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "python3 -m uvicorn agent.server:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
